# CLI-024 — migration engine continuation

## Scope and ownership

- Base: `8986a3db3d60d37b89368d37e15f1e98c60672f2`.
- Reference: `4e582f28`, Go `go1.26.6`; migration and scheduler production
  sources are unchanged between the reference and base.
- Writer: branch `rust/cli024-engine`, worktree `.worktrees/cli024-engine`.
- Corpus/replay/integration: branch `rust/cli024-integration`, worktree
  `.worktrees/cli024-integration`. No source changes in the shared main checkout.
- Scope: `internal/migration` behavior, not Go removal, a release, a product
  migration or changes to installed operator data. Existing Rust scheduler,
  serde JSON, SHA-256 and standard filesystem support are reused; no new crate.

## Recorded contract

The existing 166 CLI records remain byte-identical. Eight records generated by
running the pinned Go binary were appended to the same corpus:

- Empty installation: JSON and text (including Go null slices).
- Data/config/encrypted-profile metadata: JSON and text.
- Separate source/destination config directories.
- Legacy cron artifact detection, with explicit binary/project paths.
- Unsupported scheduler platform and nested roots, both rejected.

`input_directories` and `input_files` store the exact deterministic, synthetic
input tree for each added case. They are not handwritten expected reports.
The generator verifies unchanged source bytes and absent destination/backup
paths for dry-runs. The Rust replay recreates those inputs and compares the
entire case directory manifest before/after, as well as stdout/stderr/exit.
Fixture paths reject absolute paths and parent traversal.

The existing filesystem case `migration` gains an executable Rust consumer:
process exit, normalized stream lengths and SHA-256, full root inventories,
file payload hashes/sizes, and Unix modes for the source, destination, state
and backup marker. The legacy source is independently checked unchanged.
Directory allocation size is deliberately excluded from equality because it
is filesystem-specific; directory names, types and Unix modes stay asserted.
Windows ACL and native platform behavior remain separate proof obligations.

## Executed preparation evidence (2026-09-22)

| Command / observation | Result |
|---|---|
| `cargo fmt --all --check` on base main | Exit 0 |
| `bash -n scripts/generate-go-oracle-fixtures.sh` | Exit 0 |
| `scripts/generate-go-oracle-fixtures.sh`, twice | Both exit 0; CLI 174, MCP 52, HTTP 19, filesystem 7 |
| SHA-256 inventory comparison across both generations | All six corpus/fixture files byte-identical |
| Compare previous CLI case array to first 166 regenerated records | Exact equality |
| Explicit-manifest `cargo test -p symeraseme-cli --test command_surface --no-run --locked --offline` | Exit 0; compiles the new replay |
| Exact `migration_oracle::migration_fixture_paths_reject_escape` | Exit 0; one test passed |
| Exact `frozen_command_surface_matches_phase_two_contract`, before engine integration | Exit 101; one test executed; rejects `migrate-empty-json` exit 1 instead of Go exit 0 |
| Exact `migration_oracle::migration_filesystem_matches_frozen_go_backup_and_state`, before engine integration | Exit 101; one test executed; reports the unimplemented engine |

Cargo metadata verified workspace root and every package manifest under the
assigned integration worktree. Acceptance uses that worktree's own `target`,
not another candidate's binaries. Pre-integration failures are intentionally
retained as red regression baselines, not successful migration evidence.

## Additional safety corpus and execution recovery

The filesystem corpus now has 11 records: the seven earlier records remain
byte-identical, and four additional genuine Go executions pin repeat-run resume,
manual secret-store handling, refusal of unsupported `--copy-secrets`, and
preservation of an incomplete backup. Each case records its input bytes/modes,
all preparation process outputs, root existence, and final filesystem manifests.
An unrecognized source file proves that backups include more than the detected
migration artifacts. Two full generations produced identical hashes for all six
corpus/fixture files. The new replay compiled under all-target/all-feature Clippy;
its path-escape regression executed and passed. Engine parity remains pending.

The managed writer stopped without code artifacts. The replacement uses the
installed Codex CLI 0.155.1 with verified ChatGPT login, explicit OpenAI model,
workspace-write sandbox and no API-key fallback. Its process is
`proc_068e417c7478`; prompt, runtime log and final report are retained under
`.worktrees/cli024-engine/target/cli024-codex-*` (log: `cli024-codex.log`).
No main-checkout edits or remote publication occurred. Corpus rejection controls
for output hashes, backup marker hashes and root existence must pass after engine
integration; their positive first run prevents an unimplemented engine from
making a negative control falsely green.

## Focused acceptance after integration

Run from the integration worktree, setting `CARGO_TARGET_DIR="$PWD/target"`
and using the absolute `--manifest-path "$PWD/Cargo.toml"`. For direct Cargo
CLI subprocess tests, set `CARGO_BIN_EXE_symeraseme-rust` to the artifact built
in that same target directory, or use the repository's nextest path.

1. Full engine and CLI crate tests, then full workspace nextest.
2. Workspace Clippy, all targets/features, locked, warnings denied; format check.
3. Go vet and tracked Go formatting check.
4. Independent final-source review and fixture rejection control.
5. Exact-head PR CI and an explicit workflow-dispatch native run: normal PR CI
   skips `rust-native`, so it does not prove Linux/macOS/Windows parity.

No engine PASS, 171/3 Rust corpus claim, integrated main SHA, remote CI success
or release completion is asserted by this preparation checkpoint.
