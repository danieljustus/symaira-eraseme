# Issue #796 recovery packet

- **Base SHA:** `80d65b64a7248d4d2f1740c1e636caecab93a88e`
- **Worktree:** `/Users/daniel/Dev/Symaira Dev/symaira-eraseme/.worktrees/issue-796`
- **Branch:** `issue/796-storage-config`
- **Timeout:** 20 minutes implementation; preserve partial work on timeout
- **Recovery point:** Existing uncommitted implementation and tests in this worktree; do not reset, clean, discard, rebase, push, or open a PR.

## Task

Recover and finish issue #796: production CLI and MCP storage opening must honor documented defaults → global TOML → project TOML → environment precedence; `SYMERASEME_DB_DIR`; `SYMERASEME_ENCRYPT_DB`; persistent default storage; safe plaintext/encrypted transitions; restart durability; WAL checkpointing; and failure cleanup. Tests must use isolated directories and no personal data or real credentials.

## Contract rows

- `DB-000`: production honors persistent default, DB_DIR and ENCRYPT_DB.
- Preserve `CRY-000`/`CRY-000B` compatibility already merged on main.

## Allowed files

Issue acceptance files only, including:

- `cmd/symeraseme/real_commands.go`
- `cmd/symeraseme/main.go`
- `cmd/symeraseme/command_surface.go`
- focused tests under `cmd/symeraseme/`
- `internal/config/config.go` and focused tests
- `internal/eventstore/encrypt.go` and focused tests
- `internal/mcp/contract_handler.go` and focused tests
- `README.md`, `docs/event-store.md`, `go.mod`, `go.sum` only where necessary
- this context packet and required review evidence

No unrelated refactors. Preserve standalone-first behavior and existing public CLI/MCP contracts unless #796 explicitly requires the change.

## Acceptance commands

1. Focused tests: `go test ./cmd/symeraseme ./internal/config ./internal/eventstore ./internal/mcp`
2. `make test`
3. `make test-race`
4. `make lint`
5. `make vet`
6. `make coverage`
7. `git diff --check`

## Required result

Finish the smallest coherent implementation, inspect every existing partial change critically, add/fix focused tests as needed, run the gates, and commit locally with an issue-scoped message. Report exact commit SHA and command results. Do not push or create a PR.
