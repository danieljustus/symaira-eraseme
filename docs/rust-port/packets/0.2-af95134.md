# Task 0.2 context packet

- **Task:** Freeze repository and toolchain baselines
- **Base SHA:** `af95134b57152edad1a0aff8a3b849b4ba6b8d59`
- **Allowed files:** `scripts/capture-go-baseline.sh`, `rust-tests/parity/**`, migration planning/status documents
- **Relevant matrix rows:** `BASE-001`–`BASE-003`
- **Implementation timeout:** 20 minutes per worker; one recovery worker after review failure
- **Acceptance commands:**
  - `bash -n scripts/capture-go-baseline.sh`
  - two captures followed by exact `.stable_fields` comparison
  - `./scripts/capture-go-baseline.sh --verify`
  - `python3 -m json.tool rust-tests/parity/baselines/v0.12.1.json`
- **Safety:** use an exported `v0.12.1` tree and isolated temporary directories; never read personal data, credentials, or the developer profile/database.
- **Recovery point:** preserve the worktree and regenerate only the baseline artifacts after a failed invariant.
