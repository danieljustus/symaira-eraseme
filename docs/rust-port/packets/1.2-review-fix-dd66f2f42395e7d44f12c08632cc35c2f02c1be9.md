# Task 1.2 spec-review recovery packet

- Issue: #803
- Task: 1.2 review recovery
- Review base: `dd66f2f42395e7d44f12c08632cc35c2f02c1be9`
- Branch: `agent/issue-803-phase1-foundation`
- Worktree: `<assigned-worktree>`
- Review verdict: `needs-fix`

## Required fixes

1. Reject absolute, root/prefix and `ParentDir` fixture paths before writing;
   add traversal and absolute-path tests.
2. Ensure stdin writing cannot block before timeout polling and stdout/stderr
   draining; write stdin concurrently, close it, and bound cleanup.
3. Make process-group capability honest on non-Unix platforms: implement a
   Windows Job Object or return an explicit unsupported-capability error and
   document/test the residual platform boundary. Keep Unix process-group safety
   narrowly documented.
4. Wire the data model and comparison path for SQLite snapshots, raw HTTP
   exchanges and raw MCP frames; do not leave `sqlite_queries` unused or make
   `main` only print a message. Keep the runner honest and fixture-driven.
5. Implement parsed JSON semantic comparison or reject the mode at validation;
   add equal and mismatch tests. Never mark identical semantic JSON as a diff.
6. Constrain normalizers to an allowlisted typed scope, require non-empty reason
   tags, reject unknown rules, and surface the reason in diagnostics.
7. Make SQLite snapshots consistent for WAL/SHM state using a backup/checkpoint
   mechanism or a safe sidecar-aware copy. Declare/provision `sqlite3` or use a
   bundled Rust library; missing capability must be explicit and testable.
8. Add RAII/bounded cleanup for sandboxes and mock servers where practical;
   preserve the existing focused tests and add regression coverage for the
   important fixes.

## Allowed scope

- `rust-tests/parity/**`
- root `Cargo.toml` and `Cargo.lock` only for declared parity dependencies
- this recovery packet

Do not modify frozen cases/fixtures, Go/Swift code, Makefile, workflows,
contract docs, credentials, real data, or other packets. Preserve the Task 1.1
and Task 1.2 commits. Run the full parity Cargo gates and the added-line risk
scan before committing. Do not claim success without a commit.
