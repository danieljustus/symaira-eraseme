# Task 1.2 context packet

- Issue: #803
- Phase: 1 — Rust foundation and black-box harness
- Task: 1.2 — Add the neutral differential harness
- Foundation SHA: `56371c65e93ad00633dc60b234573bf8452c917b`
- Branch: `agent/issue-803-phase1-foundation`
- Worktree: `<assigned-worktree>`

## Acceptance slice

Create a language-neutral, data-driven parity harness that launches Go and
Rust implementations as separate process groups with isolated HOME/XDG/CWD,
allowlisted environment, timeout cleanup, raw status/signal/stdout/stderr
capture, recursive filesystem manifests with type/mode/hash, SQLite schema and
ordered-query snapshots, mock HTTP exchanges, raw MCP frames, and narrow
reason-tagged normalizers. A deliberately different dummy output must fail with
an actionable field-level diff; identical fixture programs must pass.

## Relevant evidence

- `docs/plans/2026-09-04-go-to-rust-implementation-plan.md`, Task 1.2
- `docs/plans/2026-09-04-go-to-rust-task-graph.json`, task `1.2`
- `docs/rust-port-contract-matrix.md`, BASE-001..003 and differential-testing rules
- Existing frozen oracle inputs under `rust-tests/parity/cases/**` and
  `rust-tests/parity/fixtures/**` must remain unchanged.

## Allowed paths

- `rust-tests/parity/Cargo.toml`
- `rust-tests/parity/src/main.rs`
- `rust-tests/parity/src/case.rs`
- `rust-tests/parity/src/process.rs`
- `rust-tests/parity/src/filesystem.rs`
- `rust-tests/parity/src/sqlite.rs`
- `rust-tests/parity/src/http.rs`
- `rust-tests/parity/src/mcp.rs`
- `rust-tests/parity/src/compare.rs`
- `Cargo.toml` only to add the parity crate as a workspace member
- this packet under `docs/rust-port/packets/`

## Forbidden scope

Do not modify frozen cases/fixtures, Go source or tests, Swift/UI code,
Makefile, CI/release workflows, contract-matrix rows, credentials, real
keychains, real databases, network endpoints, or the default branch.

## Verification

```text
cargo +1.98.0 fmt --all --check
cargo +1.98.0 check --workspace --all-targets
cargo +1.98.0 clippy --workspace --all-targets -- -D warnings
cargo +1.98.0 test --workspace --all-targets
cargo +1.98.0 test -p parity
```

The focused tests must demonstrate both equal and intentionally mismatching
program outputs. Preserve signal identity and use bounded timeout cleanup;
do not claim process-tree safety from killing only a direct child.

## Execution and recovery

Implementation budget: 20 minutes. Review budget: 10 minutes per lane.
If a platform-specific process-group behavior cannot be proven on this host,
keep the API explicit and record the exact unverified platform in residual risk;
do not silently weaken the contract.
