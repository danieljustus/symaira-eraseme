# Task 1.3 context packet

- Issue: #803
- Phase: 1 — Rust foundation and black-box harness
- Task: 1.3 — Add dual-language Make targets
- Foundation SHA: `6ae41eaab695de09e603e21d747f54a87c911a10`
- Branch: `agent/issue-803-phase1-foundation`
- Worktree: `<assigned-worktree>`

## Acceptance slice

Preserve the existing Go `build`, `test`, `coverage`, `lint`, `fmt-check`,
`vet` and `clean` semantics until cutover. Add explicit `build-go`, `go-gate`,
`build-rust`, `rust-gate`, `parity`, `app-test`, and `release-dry-run` targets.
Keep Go and Rust output paths separate, prevent stale binary reuse, and make
`clean` remove generated Rust/Go test artifacts without touching committed
parity cases or fixtures. New targets must fail honestly when a required
capability is absent; no fake-success placeholder handlers.

## Relevant evidence

- `docs/plans/2026-09-04-go-to-rust-implementation-plan.md`, Task 1.3
- `docs/plans/2026-09-04-go-to-rust-task-graph.json`, task `1.3`
- `docs/rust-port-contract-matrix.md`, BASE-001..003
- `Makefile`, current Go target semantics
- `rust-tests/parity/`, the committed neutral harness and frozen fixtures

## Allowed paths

- `Makefile`
- this packet under `docs/rust-port/packets/`

## Forbidden scope

Do not modify Rust source/manifests, parity cases/fixtures, Go source/tests,
Swift/UI code, CI/release workflows, contract docs, credentials, real data, or
other packets. Do not change the default branch.

## Verification

```text
make fmt-check
make test
make lint
make vet
make build
make build-go
make go-gate
make build-rust
make rust-gate
make parity
make clean
```

Run `make app-test` and `make release-dry-run` when host prerequisites exist;
otherwise ensure each target reports a concrete missing-capability error rather
than returning success. Confirm committed parity fixtures remain unchanged and
run the added-line secret/path risk scan before commit.

## Quality/security recovery evidence

- The shell-quote macro is applied to all user-overridable executable,
  environment, flag, and output-path variables used by Make recipes.
- `make -n` with malicious-looking `GO`, `GOFLAGS`, `CGO_ENABLED`, `LDFLAGS`,
  `MAKE`, `CARGO`, `GORELEASER`, `SWIFT`, `COVERAGE_FILE`, and output-path
  overrides showed each value as one quoted shell word; no injection sentinel
  was created.
- A probe executable received literal semicolon/backtick values for
  `CGO_ENABLED`, `GOFLAGS`, and `LDFLAGS` during `build-go`; all sentinels
  remained absent.
- A coverage probe accepted a `COVERAGE_FILE` containing spaces, a quote, and
  a semicolon, and the quoted removal, coverprofile, and awk paths completed
  without executing the embedded text.
- With `golangci-lint` hidden, `MAKE='make; touch <sentinel>' make lint`
  rejected the malformed executable as expected and did not create the
  sentinel.

## Execution and recovery

Implementation budget: 20 minutes. Keep the diff minimal and explain any
host-only target boundary in the commit/verification evidence. Preserve the
verified Task 1.1/1.2 commits and commit this slice locally after all applicable
gates pass.

## Quality/security recovery

- Quote all user-overridable executable/environment/flag variables in every
  Go recipe; variable contents must never become shell syntax.
- Quote `COVERAGE_FILE` in removal, coverprofile and awk paths, and use an
  option terminator for deletion where supported by the project platforms.
- Add focused shell-level regression checks proving malicious-looking
  overrides are passed as values or rejected, never executed.
