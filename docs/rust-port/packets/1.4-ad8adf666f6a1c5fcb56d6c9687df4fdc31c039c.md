# Task 1.4 context packet

- Issue: #803
- Phase: 1 — Rust foundation and black-box harness
- Task: 1.4 — Add shadow Rust CI
- Foundation SHA: `ad8adf666f6a1c5fcb56d6c9687df4fdc31c039c`
- Branch: `agent/issue-803-phase1-foundation`
- Worktree: `<assigned-worktree>`

## Acceptance slice

Add a Rust workflow that keeps the existing Go CI required and unchanged. The
Ubuntu PR gate must run Rust format, check, Clippy with warnings denied, tests,
doctests and the neutral parity subset. Main/schedule runs must add native
macOS/Linux/Windows coverage where the current harness capability allows it;
unsupported process-tree capabilities must fail honestly or be excluded from
that parity subset, never silently claim coverage. Add Rust dependency audit,
license/source policy (`cargo deny`), feature checks and safe Cargo caching.
Do not add a path filter that makes a required PR check disappear. Add Cargo to
Dependabot. Read back current required contexts (`lint`, `test (3.12)`,
`schema-validate`, `secrets-scan`) and do not change branch protection in this
slice or invent a new required context before it has run successfully on main.

## Relevant evidence

- `docs/plans/2026-09-04-go-to-rust-implementation-plan.md`, Task 1.4
- `docs/plans/2026-09-04-go-to-rust-task-graph.json`, task `1.4`
- `docs/rust-port-contract-matrix.md`, BASE-001..003
- Existing `.github/workflows/ci.yml`, `go-ci.yml`, `codeql.yml`
- Existing `.github/dependabot.yml`
- Current branch-protection contexts verified by the coordinator

## Allowed paths

- `.github/workflows/rust-ci.yml`
- `.github/dependabot.yml`
- this packet under `docs/rust-port/packets/`

## Forbidden scope

Do not modify Go workflows, CodeQL, release workflows, branch protection,
Rust source/manifests, Makefile, parity cases/fixtures, application code,
credentials, or other packets. Do not change the default branch.

## Verification

```text
actionlint .github/workflows/rust-ci.yml
cargo +1.98.0 fmt --all --check
cargo +1.98.0 check --workspace --all-targets
cargo +1.98.0 clippy --workspace --all-targets -- -D warnings
cargo +1.98.0 test --workspace --all-targets
cargo deny check
make go-gate
make rust-gate
make parity
```

Validate YAML/expressions, action pinning, PR-vs-main conditions, matrix
coverage, cache keys including Cargo.lock, and that the Rust workflow does not
replace or rename existing required Go checks. Run an added-line secret/path
risk scan before committing.

## Execution and recovery

Implementation budget: 20 minutes. CI failures caused by unavailable action
versions, runner capabilities or tool installation must be fixed or reported
with the exact workflow evidence; do not weaken gates to manufacture green.
Commit locally after fresh local checks and review.
