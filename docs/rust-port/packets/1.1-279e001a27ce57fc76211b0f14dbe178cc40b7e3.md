# Task 1.1 context packet

- Issue: #803
- Phase: 1 — Rust foundation and black-box harness
- Task: 1.1 — Create the pinned Cargo workspace
- Foundation SHA: `279e001a27ce57fc76211b0f14dbe178cc40b7e3`
- Branch: `agent/issue-803-phase1-foundation`
- Worktree: `/Users/daniel/Dev/Symaira Dev/symaira-eraseme/.worktrees/issue-803-phase1`
- Reference contract: Go remains runnable and authoritative; Rust is shadow-only.

## Acceptance slice

Create the internal three-crate Cargo workspace with Rust `1.98.0`, explicit
`rust-version = "1.98"`, committed `Cargo.lock`, dependency policy, and empty
safe crates. The shadow binary must be named `symeraseme-rust`; do not replace
or alter the Go `symeraseme` binary. Every production crate starts with
`#![deny(unsafe_code)]`.

## Relevant evidence

- `docs/plans/2026-09-04-go-to-rust-implementation-plan.md`, Task 1.1
- `docs/plans/2026-09-04-go-to-rust-task-graph.json`, task `1.1`
- `docs/rust-port-contract-matrix.md`, BASE-001..003 and the shadow-build rule
- `docs/plans/2026-09-04-go-to-rust-migration-proposal.md`, sections 3–5

## Allowed paths

- `rust-toolchain.toml`
- `Cargo.toml`
- `Cargo.lock`
- `deny.toml`
- `crates/symeraseme-core/Cargo.toml`
- `crates/symeraseme-core/src/lib.rs`
- `crates/symeraseme-engine/Cargo.toml`
- `crates/symeraseme-engine/src/lib.rs`
- `crates/symeraseme-cli/Cargo.toml`
- `crates/symeraseme-cli/src/main.rs`
- `.gitignore`
- this packet under `docs/rust-port/packets/`

## Forbidden scope

Do not modify Go source, Go fixtures, Swift/UI code, release workflows,
Makefile, dependency versions outside the new Cargo workspace, contract rows,
future Rust modules, credentials, real user data, or the default branch.

## Verification

Run serially with the pinned toolchain:

```text
rustup toolchain install 1.98.0 --profile minimal --component rustfmt,clippy
cargo +1.98.0 fmt --all --check
cargo +1.98.0 check --workspace --all-targets
cargo +1.98.0 clippy --workspace --all-targets -- -D warnings
cargo +1.98.0 test --workspace --all-targets
make fmt-check
make test
make lint
make vet
make build
```

A full Go check may be run after the Rust checks; it must remain unchanged.
Do not add a fake success handler or claim parity from compilation alone.

## Execution and recovery

Implementation budget: 20 minutes. Review budget: 10 minutes per lane.
If interrupted, preserve the worktree and report the exact HEAD, status and
partial diff. The coordinator verifies the artifact before any recovery.
