# Task 2.4 context packet

- Issue: #804
- Phase: 2 — Surface skeleton and deterministic foundations
- Task: 2.4 — Scaffold the complete Clap command tree
- Foundation SHA: `698ed6a5803b60e9ca2b9a26fb74b160dab6bdc3`
- Branch: `agent/issue-804-phase2-foundations`
- Worktree: `<assigned-worktree>`

## Authoritative oracles

- `rust-tests/parity/cases/cli/surface.json`
  - schema v1
  - Go provenance commit `bf53346cdd1297331ecda6fcb30e2c402fc3afdb`
  - 51 command nodes, 132 declared flags, 13 positionals, one hidden command
- `rust-tests/parity/cases/cli/behavior.json`
  - 165 behavior cases
  - 51 help, 51 unknown-flag, 16 success, 3 missing-argument,
    1 root-version, 1 unknown-command and 42 operational cases
- Current Go executable for direct byte comparisons.

Do not rewrite oracle expectations to fit Clap. Where Clap differs from Cobra,
add narrow compatibility rendering/error handling and prove exact bytes.

## Acceptance

1. Define every visible command, hidden command/alias, positional, local flag,
   inherited global flag and default from `surface.json`.
2. Preserve root public binary name `symeraseme`; never expose
   `symeraseme-rust` in help/errors.
3. Pass every help, unknown-flag, missing-argument, completion, root and version
   parser case that belongs to Phase 2.
4. Implement `config show` through the Task-2.3 Rust config core using explicit
   process HOME/CWD/environment capture at the CLI boundary.
5. Match CLI-009 bytes exactly for an isolated default environment:
   - text stdout: `data_dir=~/.local/share/symeraseme\nport=8000\n`
   - JSON stdout: `{"config":{"data_dir":"~/.local/share/symeraseme","db_dir":"","encrypt_db":false,"port":8000,"allow_remote":false},"success":true}\n`
   - JSON is selected with global `--output json` before `config`, between
     `config` and `show`, or after `show`; all three placements match.
   - `config show --json` remains Exit 1 with stderr
     `unknown flag: --json\n`.
6. Unported handler cases must route to one internal fail-closed boundary with a
   nonzero status and deterministic non-sensitive stderr. Never emit fake
   success, create state, access network, or execute Go.
7. The committed parser/help test must run Rust as an isolated subprocess and
   compare status/stdout/stderr exactly with the frozen Go-generated oracle for
   the selected 120 parser/help/config/version/root/completion/serve cases.
   A coordinator direct run must additionally execute both current Go and Rust
   binaries across the same 120 cases. Explicitly classify the 45 backend
   handler cases as deferred, not passing.
8. Completion output must be deterministic and match the oracle cases without
   shelling out to Go.
9. Keep CLI parsing free of global mutable state; tests use isolated HOME/CWD,
   cleared environments, bounded output and process cleanup.

## Allowed scope

- `crates/symeraseme-cli/src/main.rs`
- `crates/symeraseme-cli/src/cli.rs`
- `crates/symeraseme-cli/src/commands/**`
- `crates/symeraseme-cli/tests/command_surface.rs`
- Cargo manifests/lockfile for pinned direct dependencies only
- optional new Phase-2 CLI differential test/oracle runner under
  `rust-tests/parity/` without modifying frozen `cases/cli/*.json`
- this packet and matching review evidence

## Forbidden scope

Do not modify existing Go production/tests, Swift/UI, registry/storage/backend
handlers, Makefile, workflows, frozen CLI corpus, credentials, real data, Tasks
2.1–2.3 behavior or other packets. Do not implement later-phase handlers.

## Verification

- Rust 1.98 fmt/check/Clippy/workspace tests
- focused command-surface differential test
- exact inventory count assertion: 51 nodes
- exact selected-case count assertion: 120, deferred count: 45
- version and config-show byte probes against Go
- `cargo deny check`
- `make go-gate`
- Windows MSVC test compile
- diff/scope/secret/absolute-path scans

## Security

Help/parser errors may echo only public command/flag names. Never include HOME,
CWD, environment values, resolved config paths, tokens or credentials in new
diagnostics. Completion and unported handlers must not perform side effects.

## Execution

Implement as one coherent Task-2.4 slice, commit locally after all gates pass,
and return exact test counts plus residual deferred-handler risk.
