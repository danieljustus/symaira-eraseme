# Task 2.1 context packet

- Issue: #804
- Phase: 2 — Surface skeleton and deterministic foundations
- Task: 2.1 — Port version and build metadata
- Foundation SHA: `5cdba4bfeffb58179f5c5586cc49d246564de0ea`
- Branch: `agent/issue-804-phase2-foundations`
- Worktree: `<assigned-worktree>`

## Acceptance slice

Implement the Rust version/build metadata surface against the Go oracle. The
shadow binary remains named `symeraseme-rust`, but its CLI name/version output
must match the installed Go contract:

- root `--version`: `symeraseme version 0.13.0\n`
- `version`: `symeraseme 0.13.0\n`
- `version --json`: `{"tool":"symeraseme","version":"0.13.0","schema_version":1}\n`

Inject the version through Cargo build metadata without timestamps, preserve
trailing newlines and JSON key order, and reject extra positional arguments.
Use a typed deterministic serializer or a safe equivalent; do not hand-wave
JSON escaping or emit the Rust package/bin name in the public contract.

## Relevant evidence

- Go oracle captured from `VERSION=0.13.0 make build-go` on the foundation base
- `cmd/symeraseme/main.go`, `newVersionCommand`, `versionValue`
- `internal/version/version.go` and its tests
- `docs/plans/2026-09-04-go-to-rust-implementation-plan.md`, Task 2.1
- `docs/plans/2026-09-04-go-to-rust-task-graph.json`, task `2.1`
- `docs/rust-port-contract-matrix.md`, CLI-002..004

## Allowed paths

- `crates/symeraseme-core/src/version.rs`
- `crates/symeraseme-cli/src/main.rs`
- `crates/symeraseme-cli/Cargo.toml` only for version-surface dependencies
- `crates/symeraseme-cli/build.rs` only if build-time injection is required
- `crates/symeraseme-cli/tests/version.rs`
- root `Cargo.toml`/`Cargo.lock` only for the declared version dependency
- this packet under `docs/rust-port/packets/`

## Forbidden scope

Do not change Go source/tests, config/time/confirmation/command-tree behavior,
parity cases/fixtures, Makefile, workflows, Swift/UI code, release scripts,
credentials, real data, or other packets. Do not change the default branch.

## Verification

```text
cargo +1.98.0 fmt --all --check
cargo +1.98.0 check --workspace --all-targets
cargo +1.98.0 clippy --workspace --all-targets -- -D warnings
cargo +1.98.0 test --workspace --all-targets
cargo +1.98.0 test -p symeraseme-cli --test version
cargo +1.98.0 run -p symeraseme-cli --bin symeraseme-rust -- --version
cargo +1.98.0 run -p symeraseme-cli --bin symeraseme-rust -- version
cargo +1.98.0 run -p symeraseme-cli --bin symeraseme-rust -- version --json
```

Compare raw stdout/stderr/exit bytes with the captured Go oracle. Run the
binary with an extra positional argument and assert the non-successful parser
behavior. Run the added-line secret/path risk scan before commit.

## Execution and recovery

Implementation budget: 20 minutes. Keep the public version contract separate
from internal crate/package names. Preserve the phase-1 harness and verify the
Go baseline remains runnable. Commit one coherent slice locally after all
checks pass.
